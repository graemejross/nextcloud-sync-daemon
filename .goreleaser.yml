version: 2
builds:
  - binary: nextcloud-sync-daemon
    main: ./cmd/nextcloud-sync-daemon
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz

changelog:
  sort: asc
